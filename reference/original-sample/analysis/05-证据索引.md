# 证据索引

## 1. 可读 bundle

- `reverse/pretty/index-CaYG1oEg.pretty.js`
- `reverse/pretty/index-D8FTRdxx.pretty.js`

主分析对象是前者。

## 2. 自动抽取结果

- `reverse/analysis/generated/summary.json`
- `reverse/analysis/generated/routes.txt`
- `reverse/analysis/generated/manifest-params.txt`
- `reverse/analysis/generated/detected-libraries.txt`
- `reverse/analysis/generated/urls.txt`
- `reverse/analysis/generated/dynamic-chunks.txt`
- `reverse/analysis/generated/telemetry-events.txt`
- `reverse/analysis/generated/analytics-events.txt`

## 3. 摘录源码文件

### `reverse/extracts/01-config-and-endpoints.js`

覆盖内容：

- 端点常量
- 服务名
- release 相关常量

### `reverse/extracts/02-auth-bootstrap-and-login.js`

覆盖内容：

- OAuth 配置
- bootstrap 解析
- 登录 / 粘贴回调流程

### `reverse/extracts/03-mcp-client-and-hooks.js`

覆盖内容：

- MCP 初始化
- `mcp_servers` hook
- 连接器接入逻辑

### `reverse/extracts/04-conductor-and-bridge.js`

覆盖内容：

- conductor 共享文件系统
- 虚拟 bash
- WebSocket bridge 连接
- agent online / replay / message 处理

### `reverse/extracts/05-execute-office-js-and-conductor-globals.js`

覆盖内容：

- `execute_office_js` 的 conductor 共享文件说明
- `send_message` 之后的工作流要求
- 运行时 globals 暴露

### `reverse/extracts/06-sheet-tools-and-chart-xml.js`

覆盖内容：

- Excel structured tools 定义
- `extract_chart_xml` 真实执行逻辑

### `reverse/extracts/07-router-and-entry.js`

覆盖内容：

- router 配置
- 根 gating 逻辑
- telemetry 初始化
- `KEYBOARD_SHORTCUT_TRIGGERED` 监听

## 4. 关键证据点

### 统一服务名

- `office-agent`

### 统一发布 SHA

- `8a3d43cefdceab518a6d097570376a752fe96819`

### OAuth 客户端

- clientId：`966eba67-8b8c-4eae-bbb3-08361d1b9292`

### 跨应用桥

- `wss://bridge.claudeusercontent.com`

### MCP 远端接口

- `https://api.anthropic.com/v1/mcp_servers`
- `https://mcp-proxy.anthropic.com/v1/mcp/<id>`

### 线上差异

`2026-04-06` 校验：

- 线上首页主 bundle：`index-CCvhk5Qz.js`
- 本地样本主 bundle：`index-CaYG1oEg.js`

## 5. 仍未完全还原的部分

1. `chartXmlExtractor-CDy1bJen.js` 懒加载块不在当前本地样本里。
2. 其他动态 import chunk 大多只在主 bundle 里能看到 hash，历史文件本体未全部落盘。
3. 因为本地样本是旧快照，线上新版本的行为可能已经有增量差异。

## 6. 后续可继续做的方向

1. 对所有 `__vite__mapDeps` 懒加载点建立功能图谱。
2. 对内嵌 system prompt 做分 surface 抽取。
3. 把 PowerPoint / Word 专属工具也单独拆文档。
4. 补抓当前线上版本做一次差分分析。

